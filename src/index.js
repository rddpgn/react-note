import React, { Component } from "react";
import ReactDOM from "react-dom";
import App from "./app.component";
let tmp = document.getElementById('root');
console.log(tmp);
ReactDOM.render(<App />, tmp);