import React, { Component } from 'react';
import './Note.scss';

export class Note extends Component {
    render() {
        return (
            <div>
                Пизда
            </div>
        )
    }
}

export default Note;
